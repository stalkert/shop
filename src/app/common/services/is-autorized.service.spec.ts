/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
//import { IsAutorisatedService } from './is-authorized.service';
//import { AngularFire } from 'angularfire2';
/*describe('IsAutorisatedService', () => {
  let service;
  let AF;
  beforeEach(() => {
      TestBed.configureTestingModule({
       declarations: [ WelcomeComponent ],
        providers:    [ {provide: UserService, useValue: userServiceStub } ]
 });
    AF = new AngularFire();
    debugger;
     service = new IsAutorisatedService(AF);
  });

  it('should return isAuthorized=false by default',  () => {
    expect(service.isAuthorized).not.toBeTruthy();
  });
});*/
